---
type: slide
transition: slide
notes: |
  Market growth chart.
---

<style>
{`
  .chart-container {
    display: flex;
    align-items: flex-end;
    height: 400px;
    gap: 2rem;
    padding-left: 2rem;
    border-left: 2px solid #333;
    border-bottom: 2px solid #333;
    position: relative;
  }
  .bar-wrapper {
    flex: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    position: relative;
  }
  .bar {
    width: 100%;
    background: linear-gradient(to top, #FF6D00, #FF9100);
    border-radius: 4px 4px 0 0;
    position: relative;
    transition: height 1s ease-out;
  }
  .bar-label {
    text-align: center;
    margin-top: 1rem;
    color: #B0BEC5;
    font-size: 0.9rem;
  }
  .bar-value {
    position: absolute;
    top: -30px;
    width: 100%;
    text-align: center;
    color: #FFFFFF;
    font-weight: 700;
  }
  .grid-lines {
    position: absolute;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    z-index: 0;
    pointer-events: none;
  }
  .grid-line {
    width: 100%;
    height: 1px;
    background: rgba(255, 255, 255, 0.05);
  }
`}
</style>

<div style={{
  display: "flex",
  flexDirection: "column",
  height: "100%",
  background: "#0a0a0a",
  color: "#FFFFFF",
  padding: "4rem"
}}>

  <div style={{ marginBottom: "2rem" }}>
    <h2 style={{ fontSize: "2.5rem", fontWeight: 700, margin: "0" }}>Market Growth</h2>
    <p style={{ color: "#B0BEC5" }}>Year over year revenue increase (in millions)</p>
  </div>

  <div className="chart-container">
    <div className="grid-lines">
      {[100, 75, 50, 25, 0].map(v => <div key={v} className="grid-line" />)}
    </div>

    {[
      { year: "2022", value: 30, height: "30%" },
      { year: "2023", value: 45, height: "45%" },
      { year: "2024", value: 60, height: "60%" },
      { year: "2025", value: 85, height: "85%" },
      { year: "2026", value: 98, height: "98%" }
    ].map((d, i) => (
      <div key={i} className="bar-wrapper">
        <div className="bar" style={{ height: d.height }}>
          <div className="bar-value">${d.value}M</div>
        </div>
        <div className="bar-label">{d.year}</div>
      </div>
    ))}
  </div>

</div>
