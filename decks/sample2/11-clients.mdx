---
type: slide
transition: slide
notes: |
  Trusted by industry leaders.
---

<style>
{`
  .client-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-template-rows: repeat(3, 1fr);
    gap: 2rem;
    height: 100%;
  }
  .client-logo {
    display: flex;
    align-items: center;
    justify-content: center;
    background: #111;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.05);
    font-size: 1.2rem;
    font-weight: 700;
    color: #444;
    transition: all 0.3s ease;
  }
  .client-logo:hover {
    background: #1E1E1E;
    color: #FF6D00;
    border-color: rgba(255, 109, 0, 0.3);
    transform: translateY(-5px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.5);
  }
`}
</style>

<div style={{
  display: "flex",
  flexDirection: "column",
  height: "100%",
  background: "#0a0a0a",
  color: "#FFFFFF",
  padding: "4rem"
}}>

  <div style={{ textAlign: "center", marginBottom: "4rem" }}>
    <h2 style={{ fontSize: "3rem", fontWeight: 700, margin: "0" }}>Trusted By Leaders</h2>
    <div style={{ width: "60px", height: "4px", background: "#FF6D00", margin: "1.5rem auto 0 auto" }} />
  </div>

  <div className="client-grid">
    {Array.from({ length: 12 }).map((_, i) => (
      <div key={i} className="client-logo">
        LOGO {i + 1}
      </div>
    ))}
  </div>

</div>
