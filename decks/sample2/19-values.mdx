---
type: slide
transition: slide
notes: |
  Our core values.
---

<style>
{`
  .values-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem;
  }
  .value-item {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
  }
  .value-icon {
    min-width: 60px;
    height: 60px;
    background: rgba(255, 109, 0, 0.1);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #FF6D00;
    border: 1px solid rgba(255, 109, 0, 0.2);
  }
  .value-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 0.5rem 0;
    color: #FFFFFF;
  }
  .value-desc {
    font-size: 1rem;
    line-height: 1.5;
    color: #B0BEC5;
  }
`}
</style>

<div style={{
  display: "flex",
  flexDirection: "column",
  height: "100%",
  background: "#0a0a0a",
  color: "#FFFFFF",
  padding: "4rem"
}}>

  <div style={{ marginBottom: "4rem", textAlign: "left" }}>
    <span style={{ color: "#FF6D00", fontSize: "0.9rem", fontWeight: 700, letterSpacing: "0.15em", textTransform: "uppercase" }}>Culture</span>
    <h2 style={{ fontSize: "3rem", fontWeight: 700, margin: "0.5rem 0 0 0" }}>Core Values</h2>
  </div>

  <div className="values-grid">
    {[
      { title: "Innovation", desc: "We constantly seek new and better ways to solve problems.", icon: "Zap" },
      { title: "Integrity", desc: "We are honest, transparent, and ethical in all our dealings.", icon: "Shield" },
      { title: "Collaboration", desc: "We believe that the best results come from working together.", icon: "Users" },
      { title: "Excellence", desc: "We strive for the highest quality in everything we do.", icon: "Star" }
    ].map((item, i) => (
      <div key={i} className="value-item">
        <div className="value-icon">
          <Icon name={item.icon} size={28} />
        </div>
        <div>
          <h3 className="value-title">{item.title}</h3>
          <p className="value-desc">{item.desc}</p>
        </div>
      </div>
    ))}
  </div>

</div>
